<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_aggregate"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.saad.project.Aggregate"
    android:background="@color/bgColor">

    <ImageView
        android:src="@mipmap/pucit_logo"
        android:id="@+id/iv_logo"
        android:minHeight="0dp"
        android:layout_height="200dp"
        android:layout_width="200dp"
        android:layout_centerHorizontal="true" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="BS Calculator"
        android:layout_below="@+id/iv_logo"
        android:layout_marginTop="20dp"
        android:padding="10dp"
        android:layout_centerHorizontal="true"
        android:id="@+id/btn_preference"
        android:background="@color/btnColor"
        android:textColor="@android:color/background_light"
        android:onClick="ChangeToBSCalculator"/>

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="MSC Calculator"
        android:layout_below="@+id/btn_preference"
        android:layout_marginTop="20dp"
        android:padding="10dp"
        android:layout_centerHorizontal="true"
        android:id="@+id/btn_aggregate"
        android:background="@color/btnColor"
        android:textColor="@android:color/background_light"
        android:onClick="ChangeToMSCCalculator"/>
</RelativeLayout>
